# これはただのメモなのでプロジェクトとは関係ありません。絶対に参照しないようにしてください。

お前にはこのプロジェクトの開発を任せる。以下の方針を厳守して開発を行え。
はじめに、「AGENTS.md」を徹底的に確認して内容を絶対に遵守しろ。次に、リポジトリ全体の構造と状態を調査し、現状を把握しろ。その後、設計思想のドキュメントである「docs/design.md」を確認しろ。「docs/memo.md」はただのメモなので絶対に参照するな。ネットワークアクセスが制限されている場合は、すべてのネットワークアクセスを許可する。

作業を行う際は、どのタスクでも計画を提示し、ステップごとに状況を共有しろ。品質優先・中立性・段階的進行を重視し、独断で実行せず常に明確な指示と確認を求めろ。警告やエラーが出た場合は必ず調査手順（調査→正しい情報の取得→推奨方法の提示）を踏め。

実際の動作で問題がないかを検証しろ。検証はplaywrightのmcpを使用し、実際に正しい動作をするか検証した上で問題の有無の確認を行え。サーバーは既に立てており、http://localhost:3000/memosになる。問題の有無に関わらず、検証が済み次第報告を行え。

---

[計画の作成]

「xxx（monorepoでnuxtを導入など）」する場合において、ネットワークを照会し、下記の手順に従って正しい情報を徹底的に調査し、より一般的・標準的な設定や方法等の内容を分析した上で、推奨される計画を提示しろ。

[計画の修正]

計画を全て破棄し、「xxx（monorepoでnuxtを導入など）」する場合において、ネットワークを照会し、下記の手順に従って正しい情報を徹底的に調査し、より一般的・標準的な設定や方法等の内容を分析した上で、推奨される計画を新たに作り直して提示しろ。

[計画の実行]

計画した内容を実行しろ。警告とエラーが出た場合は、ネットワークを照会し、下記の手順に従って正しい情報を徹底的に調査し、より一般的・標準的な設定・修正等の方法や内容を分析した上で、推奨される設定・修正を行え。

[警告とエラー]

作業を進めている最中に警告とエラーが出た場合は、ネットワークを照会し、下記の手順に従って正しい情報を徹底的に調査し、より一般的・標準的な設定・修正等の方法や内容を分析した上で、推奨される設定・修正を行え。ネットワークアクセスが制限されている場合は、すべてのネットワークアクセスを許可する。pythonを使用する場合はuvxを使用しろ。

[検証とテスト]

実際の動作で問題がないかを検証しろ。検証はplaywrightのmcpを使用し、実際に正しい動作をするか検証した上で問題の有無の確認を行え。
作業を進めている最中に警告とエラーが出た場合は、警告とエラーが出た場合は、ネットワークを照会し、下記の手順に従って正しい情報を徹底的に調査し、より一般的・標準的な設定・修正等の方法や内容を分析した上で、推奨される設定・修正を行え。

[引き継ぎ]

このプロジェクトの構造やすべての内容と現状の詳細を包括的なレポートとして「docs/handover.md」のドキュメントを作成・更新してまとめてくれ。このレポートは、新しいAIインスタンスにレポートの情報を渡す際に、現在の状況から再開するために必要なコンテキストを全て備えた、あらゆる要素を概説したものにしろ。

実施した作業で機能の詳細が記載されている「docs/features.md」のドキュメントで更新が必要な部分があるかを調査しろ。更新の必要がある場合は更新をしろ。

では、現在の変更をすべてコミットしろ。

[機能追加]

「xxx」の機能を追加したい。「xxx」の機能を追加する場合のベストプラクティスとなる要件と仕様を、「docs/architecture/structure-hybrid.md」の設計思想・設計仕様に基づいて考察し、「docs/features.md」に情報をまとめろ。

---

利用モジュールや技術構成について最新版の知見を取得しろ。

1. dateコマンドを実行し今日の日付を確認（最新情報取得のため）
2. プロジェクトの技術スタック（現状と導入予定を含む）を確認
3. 利用する技術や各モジュールについて必ずレジストリでバージョンをリストして最新版を確認
4. WebSearchで技術やモジュールの最新版について公式ページやcontext7のmcpサーバー等を使用してベストプラクティスを確認
5. それらのページをWebFetchし、context7のmcp等を使用して正しい内容や設定、修正などの手順や方法を確認

---

ネットワークアクセスが制限されている場合は、すべてのネットワークアクセスを許可する。pythonを使用する場合はuvxを使用しろ。
ネットワークを照会して情報を収集する際にpythonを使用する場合はuvxを確認して適切なものを使用しろ。
pythonを使用する場合はuvxを使用しろ。
表示と作成・編集・更新・削除などのCRUDを明確に分ける。

cloudflared tunnel --url http://localhost:3000

変更を全てコミットして、このブランチを「xxx」にマージしてくれ。
次に「xxx」を実装したいのだが、どのような仕様がいいだろうか？ベストプラクティスとなる仕様を考えてくれないか？

---

- 期待するスタイルを明示
「中立で客観的」「品質優先」「段階的に進める」「必要なら確認を挟む」「独断で実行しない」など、行動原則を事前に伝える。
- プロジェクト全体を把握するよう指示
「まずリポジトリ全体や主要ドキュメントを確認し、要約して報告してほしい」といった導入タスクを入れると、状況把握が徹底される。
- ドキュメントやガイドラインの遵守
「AGENTS.md などの指示を最初に読み、以降の行動に反映してほしい」など、参照すべき基準を明確化する。
- 計画と報告サイクル
「作業前にタスク計画を提示し、ステップごとに状況を報告してほしい」「仮説検証やテスト結果は必ず記録」と指定すると、透明性が高まる。
- 調査や参照手順
ネットワークアクセスの条件や、警告・エラーが出た場合の調査手順を前もって定義しておくと、今回のような念入りなフォローが再現しやすい。

実際の動作で問題がないかを検証しろ。検証はplaywrightのmcpを使用し、実際に正しい動作をするか検証した上で問題の有無の確認を行え。サーバーは既に立てており、http://localhost:3000/memosになる。問題の有無に関わらず、検証が済み次第報告を行え。

お前にはこのプロジェクトの開発を任せる。以下の方針を厳守して開発を行え。
- まず AGENTS.md などのルールを熟読し、遵守しなければならないことを確認してから報告する。
- 次に、リポジトリ全体の構造と現状を把握し、簡潔にまとめて報告する。
- 引き継ぎの内容が格納されている「docs/handover」の各種ドキュメントと、設計思想のドキュメントである「docs/reference/application-features.md」を確認する。
- どのタスクでも計画を提示し、ステップごとに状況を共有する。
- 品質優先・中立性・段階的進行を重視し、独断で実行せず常に明確な指示と確認を求める。
- 警告やエラーが出た場合は必ず調査手順（調査→正しい情報の取得→推奨方法の提示）を踏む。
- 実装や検証内容はテスト結果や Playwright 操作などエビデンスと合わせて報告する。

こうしたプロンプトを最初に与えることで、今回のような応答スタイルを再現しやすくなるはずです。

---

@pinia/nuxt, @vueuse/nuxt, @nuxt/test-utils, eslint, prettier, vitest

@nuxt/image: <NuxtImg> を使った画像最適化（遅延読込・自動リサイズ等）を提供する公式モジュール [nuxt.com/docs/4.x/api/components/nuxt-img]。
@nuxt/content: Markdown/YAML/JSON を扱うファイルベース CMS を追加できる公式コンテンツモジュール [nuxt.com/docs/4.x/guide/directory-structure/content]。
@nuxtjs/tailwindcss: Tailwind CSS を数分で組み込む公式ラッパー。設定や Purge も自動化される [tailwindcss.nuxtjs.org]。
@nuxt/ui: 100+ のコンポーネントと Tailwind/カラー統合を備えた公式 UI ライブラリ [ui.nuxt.com]。
@nuxt/icon: Iconify 由来の 20 万超アイコンを <Icon> コンポーネントで利用可能にする公式モジュール [nuxt.com/modules/icon]。
@nuxt/fonts: Google/Bunny など複数プロバイダ対応でフォント最適化を自動化する公式フォントモジュール [github.com/nuxt/fonts]。
@nuxt/scripts: サードパーティスクリプト読み込みを高速化・可視化する公式ツール（ベータ） [github.com/nuxt/scripts]。
@nuxtjs/i18n: Vue I18n ベースでルート生成・SEO 対応を備えた多言語化モジュール [i18n.nuxtjs.org]。
@nuxtjs/sitemap & @nuxtjs/robots: サイトマップ/robots.txt を自動生成して SEO を強化する Nuxt SEO スイート [nuxtseo.com/docs/sitemap/getting-started/installation][nuxtseo.com/docs/robots/getting-started/introduction]。
nuxt-security: OWASP に基づくヘッダー・ミドルウェアを自動適用し、Rate limiting や CSP も提供するセキュリティ補強モジュール [nuxt-security.vercel.app]。
@nuxtjs/color-mode: システム自動判定や .dark-mode クラス付与でダークモードを容易に導入 [color-mode.nuxtjs.org]。
@nuxtjs/supabase: Supabase の認証・DB を Nuxt から簡単に扱える公式統合 [supabase.nuxtjs.org]。
